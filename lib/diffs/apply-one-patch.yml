---
# This file contains the three steps to be run for a single patch item.

# 1. Ensure directory exists
- name: "Ensure directory exists"
  ansible.builtin.file:
    path: "{{ dirname }}"
    state: directory
    mode: "0755"
# # 2. Ensure target file exists (create empty file if missing)
# - name: "Ensure file exists"
#   ansible.builtin.file:
#     path: "{{ dirname }}"
#     state: touch
#     mode: "0644"

# # 3. Apply patch
# - name: "Apply patch"
#   ansible.builtin.patch:
#     src: "{{ item.src }}"
#     dest: "{{ item.dest }}"
#     backup: yes
